# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
.env.local
.env.*.local
!.env.example

# Local-only notes / password files (NEVER commit these)
*PW*.txt
*password*.txt
*secret*.txt
FCESupa*.txt

# Supabase
supabase/.branches/
supabase/.temp/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Node (edge function build artifacts)
node_modules/
supabase/functions/embed/node_modules/

# Stray artifact from earlier shell invocation
nul

# Vendored third-party tool, not part of this repo. Its config.toml holds live
# DeepSeek + Pexels API keys in plaintext — never track this tree.
MoneyPrinterTurbo/

# Local image-generation runtime and its 2 GB installer archive. The portable
# tree may contain provider configuration and is not application source.
ComfyUI_windows_portable/
ComfyUI_windows_portable_*.7z

# Unrelated pre-existing files (local briefs / prompts)
PROMPT.txt
Content Creation Project folder con.txt

# Video workspaces: track the source (STORYBOARD.md, compositions/*.html,
# frame.md, hyperframes.json), never the rendered or generated binaries.
videos/local-test/
videos/**/renders/
videos/**/assets/voice/
videos/**/assets/cinematic/
videos/**/thumbnail.jpg
*.mp4
*.wav
*.mp3
